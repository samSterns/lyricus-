import React from 'react';
import IconBreadcrumbs from '../components/Breadcrumb';

const Header = () => {
  return (
    <header>
      <IconBreadcrumbs />  
    </header>
  );
};

export default Header;
